import React from "react";

import SideMenu from "../../../Navigation/Menus/SideMenu";
import { render } from "../../../../utils/__testsTools__/renderMethodRTL/customRenderMethod";

// it("SideMenu should be hidden", () => {
//   const { container } = render(<SideMenu isOpen={false} />);

//   expect(container.getElementsByClassName("bm-menu-wrap").length).toBe(1);
//   expect(container.getElementsByClassName("bm-menu-wrap")[0]).toHaveAttribute("hidden", "true");
// });

// it("SideMenu should be displayed", () => {
//   const { container } = render(<SideMenu isOpen={true} />);

//   expect(container.getElementsByClassName("bm-menu-wrap").length).toBe(1);
//   expect(container.getElementsByClassName("bm-menu-wrap")[0]).not.toHaveAttribute("hidden", "false");
// });
